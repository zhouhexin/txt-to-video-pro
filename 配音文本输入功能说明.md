# 🎙️ 配音文本输入功能

**更新日期**: 2026-04-27  
**版本**: v1.4

---

## 📋 功能说明

配音功能已升级，支持用户**手动输入每个分镜的配音文本**，不再局限于使用画面描述。

---

## 🎯 使用方式

### 方式 1: 手动输入（推荐）

1. 进入**成果展示**页面
2. 点击"查看成果"
3. 找到"🎙️ 添加配音"模块
4. 在每个分镜的输入框中输入配音文本
5. 选择音色
6. 点击"🎙️ 生成所有分镜配音"

### 方式 2: 从画面描述填充

1. 点击"🔄 从画面描述填充"按钮
2. 系统自动填充画面描述到配音文本
3. 可以手动修改任意分镜的文本
4. 点击"🎙️ 生成所有分镜配音"

---

## 🎨 界面说明

```
┌─────────────────────────────────────────────┐
│ 🎙️ 添加配音                    [最后一步]   │
├─────────────────────────────────────────────┤
│ ℹ️ 配音生成说明                              │
│ • 请为每个分镜输入配音文本                   │
│ • 生成时间：约 2-5 秒/100 字                 │
│ • 完全免费（Edge TTS）                       │
│                                             │
│ ──── 配音文本 ────                          │
│ 📝 配音文本                  [🔄 从画面描述填充]│
│                                             │
│ 镜头 1                                       │
│ ┌─────────────────────────────────────────┐ │
│ │ 请输入该分镜的配音文本...                │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 镜头 2                                       │
│ ┌─────────────────────────────────────────┐ │
│ │ 请输入该分镜的配音文本...                │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ ...                                         │
│                                             │
│ 选择音色：[xiaoxiao - 温柔女声 ▼]          │
│ 试听音色：[🔊 试听示例]                     │
│                                             │
│ [🎙️ 生成所有分镜配音]                      │
└─────────────────────────────────────────────┘
```

---

## 🔄 文本来源优先级

生成配音时，文本来源的优先级：

1. **用户手动输入** ⭐（最高优先级）
2. 剧本的 `voiceover` 字段
3. 剧本的 `narration` 字段
4. 剧本的 `visual` 字段（画面描述）

---

## 📊 数据结构

### 剧本分镜结构

```json
{
  "shots": [
    {
      "visual": "清晨的阳光洒在湖面上",
      "narration": "这是一个美丽的清晨",
      "voiceover": "清晨，阳光温暖"  // 新增：专门的配音文本字段
    }
  ]
}
```

### API 请求格式

```json
POST /api/v1/audios/generate-all
{
  "task_id": "task_xxx",
  "voice_id": "xiaoxiao",
  "voiceovers": [
    {
      "shot_index": 0,
      "text": "用户输入的配音文本 1"
    },
    {
      "shot_index": 1,
      "text": "用户输入的配音文本 2"
    }
  ]
}
```

---

## ✅ 功能特性

### 1. 独立输入

每个分镜都有独立的配音文本输入框，可以精确控制每个镜头的配音内容。

### 2. 批量填充

点击"从画面描述填充"按钮，可以快速填充所有分镜的配音文本，然后进行微调。

### 3. 自动保存

生成的配音会自动保存，下次打开页面时会自动加载已生成的配音文本。

### 4. 灵活修改

可以随时修改配音文本，重新生成配音，覆盖之前的版本。

### 5. 输入验证

- 至少需要输入一个分镜的配音文本
- 空文本的分镜会被跳过
- 支持多行文本输入

---

## 🎯 使用场景

### 场景 1: 文旅宣传片

```
镜头 1: "欢迎来到美丽的云南"
镜头 2: "这里有壮丽的山川"
镜头 3: "和清澈的湖泊"
镜头 4: "等待您的探索"
```

### 场景 2: 产品解说

```
镜头 1: "这款产品的核心特点是..."
镜头 2: "采用先进的工艺技术"
镜头 3: "确保每一个细节都完美"
镜头 4: "为您带来极致体验"
```

### 场景 3: 故事叙述

```
镜头 1: "从前，有一个美丽的传说"
镜头 2: "讲述着勇气与智慧的故事"
镜头 3: "历经千辛万苦"
镜头 4: "最终找到了幸福的归宿"
```

---

## 📝 最佳实践

### 1. 文本长度

- 每个分镜建议 20-100 字
- 总时长控制在视频时长的 80% 左右
- 留出 BGM 和音效的空间

### 2. 语言风格

- 口语化，易于朗读
- 避免生僻字和专业术语
- 注意节奏和停顿

### 3. 与画面配合

- 配音内容要与画面匹配
- 可以使用"从画面描述填充"作为起点
- 然后进行润色和优化

### 4. 试听调整

- 生成后先试听
- 不满意可以修改文本重新生成
- 调整音色直到满意

---

## 🔧 技术实现

### 前端组件

**文件**: `frontend/src/components/VoiceoverConfig.vue`

**新增功能**:
- `shots` 数组：存储用户输入的配音文本
- `initShots()`: 初始化输入框
- `handleFillFromVisual()`: 从画面描述填充
- `loadTextFromVoiceovers()`: 加载已有配音文本

### 后端接口

**文件**: `backend/app/routes/audios.py`

**修改**:
- `generate_all_audios` 接口支持 `voiceovers` 参数
- 优先使用用户输入的文本
- 回退到剧本字段

---

## ❓ 常见问题

### Q1: 可以不输入配音文本吗？

**A**: 可以！点击"从画面描述填充"按钮，系统会自动使用画面描述作为配音文本。

### Q2: 输入框可以调整大小吗？

**A**: 可以！输入框支持垂直方向拖拽调整大小，方便编辑长文本。

### Q3: 修改文本后需要重新生成吗？

**A**: 是的，修改文本后需要点击"生成所有分镜配音"重新生成。

### Q4: 可以只生成部分分镜的配音吗？

**A**: 当前实现会生成所有有文本的分镜。空文本的分镜会被跳过。

### Q5: 配音文本有长度限制吗？

**A**: 建议每个分镜 20-100 字，但没有硬性限制。Edge TTS 支持较长文本。

---

## 🎉 更新日志

### v1.4 (2026-04-27)

- ✅ 支持用户手动输入配音文本
- ✅ 每个分镜独立输入框
- ✅ 从画面描述填充功能
- ✅ 自动加载已有配音文本
- ✅ 输入验证和错误提示
- ✅ 多行文本输入，支持调整大小

### v1.3 (2026-04-27)

- ✅ 修复多分镜配音拼接问题
- ✅ 优先返回带配音的合并视频
- ✅ 成果展示页面自动加载配音

---

**开发团队**: 媒媒 AI 视频生成平台  
**技术支持**: https://github.com/zhouhexin/txt-to-video-pro
